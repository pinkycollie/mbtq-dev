# This workflow uses actions that are not certified by GitHub.
#{
  "name": "mbtq-dev",
  "version": "1.0.0",
  "scripts": {
    "build": "deno task build",
    "test": "deno test"
  }
}

      - name: Run tests
        run: deno test -A
