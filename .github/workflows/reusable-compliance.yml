# mbtq-dev/.github/workflows/reusable-compliance.yml
name: Reusable Compliance Check
on:
  workflow_call: # This makes it "blendable" into other repos
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run VR Milestone Validation
        run: deno run -A ./scripts/smoke_test.ts
